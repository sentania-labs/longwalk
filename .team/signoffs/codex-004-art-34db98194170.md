---
reviewed_branch: codex/004-art
reviewed_sha: 34db9819417e5e31ff1b047dd70aa6167a52d70d
reviewed_by: claude-worker
authored_by: codex-worker
timestamp: 2026-07-17T16:18:26Z
tests_run: tools/run_tests.sh
result: signed-off
---

Layer-1 pre-integration peer review of the round-004 art slice (decision 006
points 2 and 3). I authored none of this; I ran and checked everything below in
this worktree.

## Scope and topology

`codex/004-art` branched from `c9a6cec` (decision 006 signed), which predates the
road and camera slices being integrated into `round/004-look-like-a-game`. So
`git diff round/004-look-like-a-game..codex/004-art` misleadingly shows
`src/sim/nav_grid.gd`, `src/sim/town_layout.gd`, `camera_rig_2d.gd`, and the
active-path tests as "removed"; that is the merge-base gap, not a revert. I
reviewed the actual owned diff `git diff c9a6cec..34db981`. It touches only
`CREDITS.md`, `assets/kenney/...`, `docs/art/...`, `project.godot`,
`src/render/town/starter_town.gd`, and `tools/art/...`. Nothing under `src/sim/`
is touched, so the constitution's sim/render separation holds and the render
flip stayed render-side. The orchestrator owns the merge of this SHA into the
current round head.

## Tests

Ran `tools/run_tests.sh` in this worktree: exit 0, all suites passed. The
art-pipeline Python tests (`test_build_player_walk.py`, `test_check_walk_sheet.py`)
ran and passed, so Pillow/PIL deps resolve here (the prior-round CI dep concern
does not reproduce in-worktree).

## Licensing (load-bearing for an art slice)

Every committed third-party asset is CC0 and credited:

- Kenney Roguelike/RPG pack (`assets/kenney/roguelike-rpg-pack/*.png`,
  `source-sheet.png`): the bundled `License.txt` states Creative Commons Zero
  (CC0), credit appreciated but not mandatory. `CREDITS.md` has an entry with
  the source URL (https://kenney.nl/assets/roguelike-rpg-pack) and CC0.
- Reference walk GIF (`docs/art/reference/human_walk_downx3.gif`): `CREDITS.md`
  names it "Human RPG Character by Shepardskin", CC0, source
  https://opengameart.org/content/human-rpg-character. I independently fetched
  that page and confirmed submitter Shepardskin and a CC0 badge ("No attribution
  necessary"). It is used only as the reference specimen in the acceptance GIF,
  not shipped in-game, but is committed and is license-clean.

No noncommercial or unclear-license asset is committed.

## Constitution conformance

- No em-dashes: grepped U+2014 across the owned diff and the commit message. None.
- Determinism: `ingest_kenney_roguelike.py`, `build_walk_comparison.py`,
  `build_player_walk.py`, `process_assets.py` use fixed tile indices, fixed
  ranges, and insertion-ordered dict iteration. No `random`, no unseeded RNG, no
  iteration-order accumulator.
- sim/render separation: confirmed above (no `src/sim/` touch).

## Synthesis conformance (decision 006)

- Render flip is nearest-neighbour at a fixed integer scale on the pack grid:
  `project.godot` sets `window/stretch/mode="viewport"` and
  `textures/canvas_textures/default_texture_filter=0`; `process_assets.py` and
  `build_player_walk.py` switch LANCZOS to NEAREST; `ingest_kenney_roguelike.py`
  enlarges the 16 px pack grid by an exact 8x to the 128 px world tile with no
  interpolation. Consistent with point 3.
- Decision 005 walk cycle is not altered: the filter change to
  `_shipping_frame` re-resamples the existing governed 4-frame atlas; frame count,
  topology, and selection are untouched. `build_walk_comparison.py` crops the
  existing `player_walk_moss.png` at 4 columns (`range(4)`), it does not
  regenerate or reselect frames. The acceptance doc states the same. This is the
  render flip applied to the player atlas, not a gait change, consistent with
  points 7 and 8 (pixelization is not passed off as a gait fix; the GIF is framed
  as Scott's evidence, not a gate).
- Road costs / camera / shadows are other slices, correctly absent here.

## Protected paths

`project.godot` is protected and is enumerated in decision 006's "Protected
paths touched". Coverage met. (`src/sim/` is also listed there but is untouched
by this slice.)

## Scott-facing artifacts

All present and coherent (I am confirming existence and coherence, not judging
the art, which is Scott's gate):

- `round-004-after.png`: PNG 1280x720. Viewed it: pixel-art traveller with a
  contact shadow, grass-to-path transitions, a pack cottage (top-right), and
  trees/bushes/flower patches y-sorted around the traveller. Matches the
  composition-proof claim.
- `round-004-before.png`: PNG 1280x720, the legacy smooth-art baseline.
- `round-004-walk-comparison.gif`: GIF 400x220, 12 frames, longwalk 4-frame
  cycle beside the CC0 3-frame reference on a shared baseline.
- `round-004-acceptance.md`: coherent verdict note; correctly scopes the proof
  to the rendering-model flip and defers gait/art-direction to Scott.

## Verdict

signed-off. The slice is correctly scoped, license-clean, deterministic,
constitution-conformant, matches the decision 006 synthesis, and ships coherent
Scott-facing artifacts. The suite passes in-worktree.
