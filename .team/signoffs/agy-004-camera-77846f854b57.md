---
reviewed_branch: agy/004-camera
reviewed_sha: 77846f854b577e2e1b69de9a28bdfba77b1fed9e
reviewed_by: claude-worker
authored_by: agy-worker
timestamp: 2026-07-17T16:06:00Z
tests_run: tools/run_tests.sh
result: signed-off
---

Layer-1 pre-PR sign-off for decision 006 point 5 (camera FOLLOW / FOCUSED rig,
right-click focus, Space return-to-follow). Reviewed the diff
`round/004-look-like-a-game..agy/004-camera` in this worktree.

## Tests

`tools/run_tests.sh` passes (exit 0). All zoom and rig checks pass, including the
five new camera-rig assertions. The two `argparse` "error:" lines in
`test_check_walk_sheet.py` are that suite's intentional negative-path assertions,
not failures, and the `get_node() absolute paths` ERROR is a pre-existing
smoke-test log line unrelated to this slice.

## Constitution conformance

- **sim/render separation:** the diff touches nothing under `src/sim/`. The new
  rig lives at `src/render/town/camera_rig_2d.gd` (`extends Camera2D`) and no
  camera/viewport dependency leaked into the sim tree.
- **determinism:** no RNG introduced; the rig is pure input/eased-follow.
- **no em-dashes:** grep for U+2014 over the whole code diff is clean.

## Synthesis conformance (decision 006 point 5)

- **Reparented off the player, genuinely.** The rig is added to `_world` as a
  sibling of the player (`starter_town.gd:230`), not a child of the player node.
  The old `scenes/player.tscn` `Camera2D` child is removed and no dangling
  `get_node("Camera2D")` references remain anywhere in `src/`, `scenes/`, `test/`.
- **Focus persists across route changes** (codex's persistence, not agy's
  defeated snap-back): `_process` holds `_focus_point` while FOCUSED and only
  tracks the player while FOLLOW. Left-click is not handled by the rig, so a move
  command cannot restore follow.
- **Space `center_on_player` returns to FOLLOW** via the new `center_on_player`
  InputMap action (keycode 32).
- **Focus is independent of player pathing:** right-click sets `_focus_point`
  from the world mouse position, clamped to camera limits, decoupled from player.

## Protected-path coverage

`project.godot` is protected (`.github/protected-paths.txt:20`) and is listed
under decision 006's "Protected paths touched" (line 194). Decision 006 is signed
by all three workers. Coverage met.

## Zoom/bounds regression re-proven by mutation (not just re-read)

Because this exact zoom path moved from the player controller into the rig, I
did not trust the assertions by reading; I mutated the rig and confirmed the
suite bites:

1. Inverted the min-zoom floor `maxf(min_zoom_x, min_zoom_y)` -> `minf(...)`:
   suite failed with "Viewport width/height at min zoom fits within town" (2
   failures). The `maxf` bounds floor is genuinely enforced.
2. Commented out the `_recompute_zoom_levels()` call in `setup()`: suite failed
   with "Viewport width at min zoom fits within town". The reparent did not
   silently drop the zoom-recompute wiring.
3. Made the FOCUSED branch track `_player.position` (breaking persistence): suite
   failed with "Camera eased to focus point" and "Camera holds focus independent
   of player move". The persistence contract genuinely bites.

All mutations reverted; worktree restored to the reviewed SHA. Signed off.
