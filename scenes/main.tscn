[gd_scene load_steps=1 format=3]

; Placeholder main scene. The real title screen, character creation, and
; starter town are built in later dispatches (see ROADMAP.md, "M3:
; starter-town prototype"). This scene only keeps the project bootable and
; exportable in the meantime; it carries no gameplay code.

[node name="Main" type="Node2D"]

[node name="Label" type="Label" parent="."]
offset_left = 32.0
offset_top = 32.0
offset_right = 700.0
offset_bottom = 80.0
text = "longwalk: starter-town prototype coming in a later dispatch"
