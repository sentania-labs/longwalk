# Round 006 anonymized acceptance evidence

This document reports independent conditions only. It does not declare the overall gate. Candidate identities remain in `_key.json`; this body uses blind subject labels.

Capture mechanism: deterministic Pillow compositing of committed finished sprites onto a fixed 960x540 shipping-zoom town composition built from the shipped ground, lane, facade, and cottage textures. All subjects use the same 1.0 pixel scale, framing, sRGB conversion, and `(1.0, 0.95, 0.88)` canvas grade. The spike is the committed fixed reference, contained without distortion. No live viewport, network operation, or paid call was used.

## Painterly fidelity: DEFER-TO-ORCHESTRATOR

Judge matched stills for `subject-4`, `subject-1`, `subject-2`, and `subject-3`. Measurable mutation evidence for both candidates is PASS: both use the same committed cleaned geometry for every frame, and the candidate B generative operation was texture-only.

## Structural preservation: DEFER-TO-ORCHESTRATOR

Judge matched stills for `subject-4`, `subject-1`, `subject-2`, and `subject-3`. Measurable mutation evidence for both candidates is PASS: both use the same committed cleaned geometry for every frame, and the candidate B generative operation was texture-only.

## Motion stability: PASS

- `subject-4`: 8/8 byte-distinct facing rows; six poses; contact-anchor max deviation 0 px (limit 2 px); normalized frame delta mean 0.0229, max 0.0414. One committed albedo and deterministic render per asset means no per-frame generative jitter. Real-gait and visual-boiling judgment: DEFER-TO-ORCHESTRATOR at `subject-4/walk.gif`.
- `subject-1`: 8/8 byte-distinct facing rows; six poses; contact-anchor max deviation 0 px (limit 2 px); normalized frame delta mean 0.0303, max 0.0506. One committed albedo and deterministic render per asset means no per-frame generative jitter. Real-gait and visual-boiling judgment: DEFER-TO-ORCHESTRATOR at `subject-1/walk.gif`.

## Scale: PASS

Both candidate player and cottage manifests pass `check_scale_contract.py`. Integration loads native 160x160 player cells and the 512x512 cottage texture with anchor offsets only, with no runtime scale transform.

- `subject-4`: player 137.17 px, door 156.77 px, eaves 188.12 px; player/door 0.875; eaves/door 1.200. These equal decision 010's 32*sqrt(6) px/m contract.
- `subject-1`: player 137.17 px, door 156.77 px, eaves 188.12 px; player/door 0.875; eaves/door 1.200. These equal decision 010's 32*sqrt(6) px/m contract.

## Grounding: PASS

- `subject-4`: 0 contact-plane alpha failures across 48 frames; estimated player light angle 90.4 deg and cottage angle 90.5 deg, difference 0.1 deg. Both were rendered with the shared fixed warm-key and cool-fill rig. Visual grounding and Two-Rivers vibe: DEFER-TO-ORCHESTRATOR at `subject-4/town.png`.
- `subject-1`: 0 contact-plane alpha failures across 48 frames; estimated player light angle 91.8 deg and cottage angle 100.5 deg, difference 8.7 deg. Both were rendered with the shared fixed warm-key and cool-fill rig. Visual grounding and Two-Rivers vibe: DEFER-TO-ORCHESTRATOR at `subject-1/town.png`.

## Production economics: PASS for recorded pilot, DEFER-TO-ORCHESTRATOR for production authorization

The shared cleanup ledger records 5 minutes of mesh cleanup per asset, 0 minutes UV repair, 0 minutes rig repair, and zero rejected delivered generations. Both candidate treatments add 0 minutes of per-frame hand work. Linear extrapolation at 5 minutes per asset is 1,000 minutes, or 16.7 hours, for about 200 assets, before review overhead. Candidate A rerender time was 13.9 to 14.0 minutes for the pilot; candidate B was 10.8 minutes. Paid balance remains the provenance-recorded 2970 because this harness uses committed local inputs only.

The orchestrator must rule the overall gate only after judging the blind stills and motion captures, then opening `_key.json`.
