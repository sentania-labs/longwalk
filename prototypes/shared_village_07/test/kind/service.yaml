apiVersion: v1
kind: Service
metadata:
  name: longwalk-smoke
spec:
  selector:
    app: longwalk-world
  ports:
    - protocol: UDP
      port: 7777
      targetPort: 7777
      nodePort: 30777
  type: NodePort
